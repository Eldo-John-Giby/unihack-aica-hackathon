// ============================================================
// API CONFIGURATION
// Change this one constant to switch from mock data to real API
// ============================================================
export const API_BASE_URL = 'http://localhost:3001/api';

// Set to false when real API is ready
export const USE_MOCK_DATA = true;

// Mock data paths (relative to frontend root)
export const MOCK_DATA_PATHS = [
  '/mock_data/product_001.json',
  '/mock_data/product_002.json',
  '/mock_data/product_003.json',
];
